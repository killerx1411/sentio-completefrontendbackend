# Analytics / video API (existing SentioMind backend)
REACT_APP_API_URL=https://api.sentiomind.in

# JWT auth service (Flask + PostgreSQL) — local dev default
REACT_APP_AUTH_API_URL=http://localhost:5000

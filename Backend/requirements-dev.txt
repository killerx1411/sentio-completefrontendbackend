# Test toolchain. Deliberately NOT in requirements.txt: pytest and its plugins
# have no business inside the production Cloud Run image.
#   pip install -r requirements.txt -r requirements-dev.txt
pytest==8.4.2
pytest-flask==1.3.0
pytest-cov==5.0.0
